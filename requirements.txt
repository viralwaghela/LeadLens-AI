streamlit==1.59.2
requests>=2.31,<3
python-dotenv>=1.0,<2
openai>=1.68,<3
python-docx>=1.1,<2
openpyxl>=3.1,<4
pandas>=2.2,<3
google-api-python-client>=2.150,<3
google-auth>=2.35,<3
google-auth-httplib2>=0.2,<1
psycopg2-binary>=2.9,<3

# V2 Phase 0: relational schema foundation (core/db/, alembic/). Additive
# infrastructure only — nothing in the live app imports these yet. See
# docs/V2_COEXISTENCE.md before wiring anything to them.
SQLAlchemy>=2.0,<3
alembic>=1.13,<2

# V2 Phase 1: identity/authorization backend (core/identity/). Argon2id
# password hashing via argon2-cffi. Not used by core/auth.py (the live
# login path) — see docs/V2_PHASE1_IDENTITY.md.
argon2-cffi>=23,<26

# V2 Phase 6: per-organization integration credential encryption
# (services/credential_encryption.py). Fernet symmetric encryption —
# already an indirect dependency via google-auth, pinned here directly
# since Phase 6 depends on it explicitly. See
# docs/V2_PHASE6_INTEGRATION_CREDENTIALS.md.
cryptography>=42,<51
